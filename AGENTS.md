# ChatGPT·Codex 업데이트 아카이브 운영 지침

## 목적

이 프로젝트는 ChatGPT와 Codex의 공식 업데이트를 한국어로 정리해 `index.html` 한 곳에 축적하는 정적 웹 아카이브다.

## 데이터 원칙

- 업데이트 기록의 단일 원본은 `index.html`의 `.update-list` 안에 있는 `article.update` 요소다.
- 날짜별 Markdown 파일을 만들거나 갱신하지 않는다.
- 기존 기록 중 가장 최신 날짜와 공식 출처 링크를 먼저 확인한 뒤, 그 이후에 발표된 새 소식만 조사한다.
- 제품명, 제목, 발표일, 핵심 변경, 사용자 영향, 공식 출처 URL을 비교해 중복을 만들지 않는다.
- 새 공식 발표가 없으면 어떤 파일도 변경하지 않는다.

## 확인할 공식 출처

1. ChatGPT 공식 릴리스 노트
2. Codex 공식 changelog
3. OpenAI Codex 공식 GitHub Releases

공식 OpenAI 문서와 `openai/codex` 저장소만 근거로 사용한다. 검색 결과 요약만 믿지 말고 실제 원문을 열어 발표일과 내용을 확인한다. 여러 출처에 같은 변경이 있으면 하나의 업데이트 카드로 합치고 모든 관련 공식 링크를 넣는다.

## HTML 갱신 방법

- 새 카드는 `index.html`의 `<section class="update-list">` 안에서 기존 카드보다 앞쪽에 최신순으로 추가한다.
- 기존 `article.update` 구조와 클래스 이름을 그대로 따른다.
- `data-product`는 `chatgpt` 또는 `codex`만 사용한다.
- 카드 헤더의 대표 날짜는 아카이브에 기록하는 확인 날짜가 아니라 공식 발표일을 우선 사용한다.
- `<time datetime>`은 `YYYY-MM-DD` 형식으로 작성한다.
- 세부 내용에는 최소한 `발표일`, 주요 변경 내용, `사용자 영향`을 포함한다.
- 기술 용어를 그대로 나열하지 말고 일반 사용자가 이해할 수 있는 한국어로 풀어쓴다.
- 카드 마지막에는 `공식 출처` 링크와 `한눈에 보기` 요약을 넣는다.
- 외부 링크에는 `target="_blank" rel="noopener noreferrer"`를 유지한다.
- 총 개수와 최신 날짜는 `script.js`가 카드에서 자동 계산하므로 HTML에 별도 집계값을 하드코딩하지 않는다.

## 변경 범위

- 업데이트 추가는 원칙적으로 `index.html`만 수정한다.
- 디자인이나 동작 변경이 명시적으로 필요한 경우에만 CSS 또는 JavaScript를 수정한다.
- `about.html`은 운영 방식 자체가 바뀔 때만 수정한다.

## 완료 전 검사

변경이 생긴 경우 다음을 모두 확인한다.

1. `article.update` 개수와 제품별 개수를 확인한다.
2. 날짜가 최신순인지, 동일 제목·발표일·출처가 중복되지 않았는지 검사한다.
3. HTML의 로컬 `src`와 `href`가 실제 파일을 가리키는지 검사한다.
4. `node --check script.js`와 `node --check about.js`를 실행한다.
5. 가능하면 브라우저에서 첫 화면, 제품 필터, 연도·월 필터, 테마 전환과 소개 페이지를 확인한다.

## 보고 방식

- 새 소식이 있으면 추가한 항목, 공식 발표일, 출처와 검사 결과를 간단히 보고한다.
- 새 소식이 없으면 파일을 변경하지 않았다고 명확히 보고한다.
